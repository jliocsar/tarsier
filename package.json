{
  "name": "tarsier",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --bun --watch index.test.ts",
    "build": "bun build index.ts --target=node --outdir=dist && tsc",
    "test": "bun test"
  },
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "colorette": "latest"
  }
}
